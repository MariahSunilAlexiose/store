#pragma once
#include "StoreManager.h"

/* MaintenanceSwitch toggles the maintenance mode of the store
* @param store the store object whose maintenance mode is to be toggled
*/
void MaintenanceSwitch(StoreManager& store);